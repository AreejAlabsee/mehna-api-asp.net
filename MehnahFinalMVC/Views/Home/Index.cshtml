@* @model MehnahFinalMVC.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home";
}

<h2 class="mb-4">Works List</h2>

@if (Model.Works != null && Model.Works.Any())
{
    <div class="row row-cols-1 row-cols-md-2 g-4">
        @foreach (var work in Model.Works)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">@work.Description</h5>
                        <p class="card-text">
                            <strong>Category:</strong> @work.Category <br />
                            <strong>Price:</strong> @work.Price <br />
                            <strong>Available:</strong> @(work.IsAvailable ? "Yes" : "No")
                        </p>
                        <a class="btn btn-primary btn-sm" href="@Url.Action("Details", "Home", new { id = work.Id })">Details</a>
                        <a class="btn btn-warning btn-sm" href="@Url.Action("Edit", "Home", new { id = work.Id })">Edit</a>
                        <a class="btn btn-danger btn-sm" href="@Url.Action("Delete", "Home", new { id = work.Id })">Delete</a>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No works available.</p>
}

<hr class="my-5" />

<h2 class="mb-4">Ratings</h2>

@if (Model.Ratings != null && Model.Ratings.Any())
{
    <div class="list-group">
        @foreach (var rating in Model.Ratings)
        {
            <div class="list-group-item list-group-item-action flex-column align-items-start mb-2 shadow-sm">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">Work ID: @rating.WorkId</h5>
                    <small>⭐ @rating.Stars</small>
                </div>
                <p class="mb-1">@rating.Comment</p>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No ratings yet.</p>
} *@
@* @model MehnahFinalMVC.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home";
}

<h2 class="mb-4">Workers</h2>

@if (Model.Users != null && Model.Users.Any())
{
    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var worker in Model.Users)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    <img src="@worker.ProfileImage" class="card-img-top" alt="@worker.Name" style="height:200px; object-fit:cover;">
                    <div class="card-body">
                        <h5 class="card-title">@worker.Name</h5>
                        <p class="card-text">
                            <strong>Phone:</strong> @worker.PhoneNumber <br />
                            <strong>Type:</strong> @worker.UserType
                        </p>
                        <a class="btn btn-primary btn-sm" href="@Url.Action("Details", "Workers", new { id = worker.Id })">Details</a>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No workers available.</p>
}

<hr class="my-5" />

<h2 class="mb-4">Works List</h2>

@if (Model.Works != null && Model.Works.Any())
{
    <div class="row row-cols-1 row-cols-md-2 g-4">
        @foreach (var work in Model.Works)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">@work.Description</h5>
                        <p class="card-text">
                            <strong>Category:</strong> @work.Category <br />
                            <strong>Price:</strong> @work.Price <br />
                            <strong>Available:</strong> @(work.IsAvailable ? "Yes" : "No")
                        </p>
                        <a class="btn btn-primary btn-sm" href="@Url.Action("Details", "Home", new { id = work.Id })">Details</a>
                        <a class="btn btn-warning btn-sm" href="@Url.Action("Edit", "Home", new { id = work.Id })">Edit</a>
                        <a class="btn btn-danger btn-sm" href="@Url.Action("Delete", "Home", new { id = work.Id })">Delete</a>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No works available.</p>
}

<hr class="my-5" />

<h2 class="mb-4">Ratings</h2>

@if (Model.Ratings != null && Model.Ratings.Any())
{
    <div class="list-group">
        @foreach (var rating in Model.Ratings)
        {
            <div class="list-group-item list-group-item-action flex-column align-items-start mb-2 shadow-sm">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">Work ID: @rating.WorkId</h5>
                    <small>⭐ @rating.Stars</small>
                </div>
                <p class="mb-1">@rating.Comment</p>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No ratings yet.</p>
} *@
@model MehnahFinalMVC.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home";
}

<h2 class="mb-4">Workers</h2>

@if (Model.Users != null && Model.Users.Any())
{
    <div class="row row-cols-1 row-cols-md-3 g-4 mb-5">
        @foreach (var worker in Model.Users)
        {
            

            <div class="card-body">
                <h5 class="card-title">@worker.Name</h5>
                <p class="card-text">
                    <strong>Phone:</strong> @worker.PhoneNumber <br />
                    <strong>Type:</strong> @worker.UserType
                </p>
            </div>
      
    
        }
    </div>
}
else
{
    <p class="text-muted">No workers available.</p>
}

<hr class="my-5" />

<h2 class="mb-4">Works List</h2>

@if (Model.Works != null && Model.Works.Any())
{
    <div class="row row-cols-1 row-cols-md-2 g-4 mb-5">
        @foreach (var work in Model.Works)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">@work.Description</h5>
                        <p class="card-text">
                            <strong>Category:</strong> @work.Category <br />
                            <strong>Price:</strong> @work.Price <br />
                            <strong>Available:</strong> @(work.IsAvailable ? "Yes" : "No")
                        </p>
                        <a class="btn btn-primary btn-sm" href="@Url.Action("Details", "Home", new { id = work.Id })">Details</a>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No works available.</p>
}

<hr class="my-5" />

<h2 class="mb-4">Ratings</h2>

@if (Model.Ratings != null && Model.Ratings.Any())
{
    <div class="list-group mb-5">
        @foreach (var rating in Model.Ratings)
        {
            <div class="list-group-item list-group-item-action flex-column align-items-start mb-2 shadow-sm">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">Work ID: @rating.WorkId</h5>
                    <small>⭐ @rating.Stars</small>
                </div>
                <p class="mb-1">@rating.Comment</p>
            </div>
        }
    </div>
}
else
{
    <p class="text-muted">No ratings yet.</p>
}
